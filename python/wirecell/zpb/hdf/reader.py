#!/usr/bin/env python3
'''
Support for reading HDF5 files.
'''

def file_handler(ctx, pipe, filename, addrpat, wargs):
    assert(False)

def client_handler(ctx, pipe, bot, rule_object, broker_addr, writer_addr):
    assert(False)
